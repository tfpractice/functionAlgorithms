import 'jasmine-expect';
import { mergeSort, mergeTail, mSort_orig, topMerge } from 'src/sort/merge';
import { testTen } from './sortData';

describe('merge', () => {
  it('merges', () => {
    // console.log('mergeSort(test)', mergeSort([ ...test ]));
    // console.log('mergeTail(test)', mergeTail(testTen));
  });
});
